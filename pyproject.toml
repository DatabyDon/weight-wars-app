[project]
name = "weight-wars"
version = "0.1.0"
description = "A Streamlit app for tracking our progress toward our weight goals."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "streamlit==1.63.0",
    "pandas==3.0.5",
    "numpy==2.5.2",
]

[tool.uv]
package = false
